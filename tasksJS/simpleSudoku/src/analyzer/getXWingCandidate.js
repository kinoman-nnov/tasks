// 2 строки или 2 столбца имеют кандидата только в 2х клетках каждой из них
// эти клетки по столбцам и строкам должны образовать прямоугольник

export function getXWingCandidate(table) {
console.log(table);

  // for (let i = 0; i < Array.from(table).length; i++) {
  //   console.log(i);
    
  // }
}